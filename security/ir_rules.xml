<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- User: Can only see their own patient records -->
    <record id="rule_hms_patient_user_own" model="ir.rule">
        <field name="name">HMS Patient: User sees own records</field>
        <field name="model_id" ref="model_hms_patient"/>
        <field name="domain_force">[('create_uid', '=', user.id)]</field>
        <field name="groups" eval="[(4, ref('hms.group_hms_user'))]"/>
        <field name="perm_read">1</field>
        <field name="perm_write">1</field>
        <field name="perm_create">1</field>
        <field name="perm_unlink">0</field>
    </record>

    <!-- Manager: Can see all patient records -->
    <record id="rule_hms_patient_manager_all" model="ir.rule">
        <field name="name">HMS Patient: Manager sees all records</field>
        <field name="model_id" ref="model_hms_patient"/>
        <field name="domain_force">[(1, '=', 1)]</field>
        <field name="groups" eval="[(4, ref('hms.group_hms_manager'))]"/>
        <field name="perm_read">1</field>
        <field name="perm_write">1</field>
        <field name="perm_create">1</field>
        <field name="perm_unlink">1</field>
    </record>

</odoo>
